# Biomni Databricks A1 Agent – install on cluster or job environment
# Do not rely on runtime pip install in the agent notebook.

# LangChain and LangGraph
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-core>=0.3.0
langgraph>=0.2.0

# Optional: DSPy for prompt optimization (used in generate node)
dspy-ai>=2.5.0

# Data and HTTP
pandas>=2.0.0
requests>=2.28.0

# Databricks (usually provided by cluster; uncomment if running outside Databricks)
# databricks-sdk>=0.40.0
# pyspark>=3.5.0
